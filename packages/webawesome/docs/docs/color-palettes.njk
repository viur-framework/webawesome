---
title: Color Palettes
description: 'Color palettes give you a full spectrum of colors to add life to your project.'
layout: page
---

<p>Color palettes give you a full spectrum of colors to use in your project.</p>

<p>A color palette defines 10 hues — red, orange, yellow, green, cyan, blue, indigo, purple, pink, and gray — each with
  a scale of 11 tints from <code>05</code> (darkest) to <code>95</code> (lightest) available as <a
    href="/docs/tokens/color">color design tokens</a>. While the tints have similar lightness values across palettes,
  each palette has unique hue shifts and chroma so you can find just the right vibe to your project. Combine a palette
  with a <a href="/docs/themes">theme</a> and <a href="/docs/tokens/color#variant-colors">variant colors</a> to style
  the whole library.</p>

{% include "theming/color-palette-picker.njk" %}

{% raw %}
{% if not currentUser or not currentUser.hasPro %}
{% endraw %}
<wa-callout class="pro">
  <wa-icon slot="icon" name="hand-wave" animation="shake"
    style="--animation-delay: 2s; --animation-duration: 4s;"></wa-icon>
  <strong>
    <a href="/purchase?from=pro-docs&context=color-palettes">Unlock every color palette with {{ site.namePro }}!</a>
  </strong>

  <wa-details appearance="plain">
    <span slot="summary">Subscribing to {{ site.namePro }} unlocks every color palette, plus premium themes, the visual
      Theme Builder, and <u class="appearance-underlined variant-drawn">more</u>.</span>

    <div class="wa-stack wa-gap-xl">
      {% include "pro-perks.njk" ignore missing %}

      <wa-button href="/purchase?from=pro-docs&context=color-palettes">
        Get {{ site.namePro }}
        <wa-icon slot="end" name="arrow-right"></wa-icon>
      </wa-button>
    </div>
  </wa-details>
</wa-callout>
{% raw %}
{% endif %}
{% endraw %}

<h2>Using This Palette</h2>

<div id="import-code">
  {% for palette in themer.palettes %}
  <div class="palette-instructions" data-palette="{{ palette.name | lower }}" {% if not loop.first %}hidden{% endif %}>
    <wa-tab-group data-install-tabs>
      <wa-tab panel="cdn"><wa-icon name="globe" variant="regular"></wa-icon> CDN</wa-tab>
      <wa-tab panel="npm"><wa-icon name="box-open" variant="regular"></wa-icon> npm</wa-tab>
      <wa-tab panel="self-hosted"><wa-icon name="arrow-down-to-line" variant="regular"></wa-icon> Self-Hosted</wa-tab>
      <wa-tab panel="hosted"><wa-icon name="rocket-launch" variant="regular"></wa-icon> Hosted Projects</wa-tab>

      <wa-tab-panel name="cdn">
        {% if palette.isPro %}
{% markdown %}
<wa-callout variant="neutral" size="s">
  <wa-icon slot="icon" name="info-circle" variant="regular"></wa-icon>
  This palette can only be used with a Pro subscription. {% raw %}{% if session.isLoggedIn %}{% endraw %}For instructions unique to your Pro project, open a project from <a href="/workspaces">your workspace</a>.{% raw %}{% endif %}{% endraw %}
</wa-callout>
{% endmarkdown %}
        {% else %}
        {% set paletteCdnUrl %}{% cdnUrl 'styles/color/palettes/' %}{{ palette.filename }}{% endset %}
{% markdown %}
To use this palette, add the palette's stylesheet from the CDN:

```html
<link rel="stylesheet" href="{{ paletteCdnUrl }}">
```

Then, apply the following class to the `<html>` element:

```html
<html class="wa-palette-{{ palette.name | lower }}">
```
{% endmarkdown %}
        {% endif %}
      </wa-tab-panel>

      <wa-tab-panel name="npm">
{% markdown %}
After installing <code>@awesome.me/webawesome</code>, import the palette's stylesheet:

```js
import '@awesome.me/webawesome/dist/styles/color/palettes/{{ palette.filename }}';
```

Then, apply the following class to the `<html>` element:

```html
<html class="wa-palette-{{ palette.name | lower }}">
```
{% endmarkdown %}
      </wa-tab-panel>

      <wa-tab-panel name="self-hosted">
{% markdown %}
If you're self-hosting Web Awesome, include the palette's stylesheet from your server:

```html
<link rel="stylesheet" href="/dist/styles/color/palettes/{{ palette.filename }}">
```

Then, apply the following class to the `<html>` element:

```html
<html class="wa-palette-{{ palette.name | lower }}">
```
{% endmarkdown %}
      </wa-tab-panel>

      <wa-tab-panel name="hosted">
        <div class="hosted-project-instructions">
          {% if palette.isPro %}
          <div class="instruction-group wa-stack wa-gap-2xs">
            <h3 class="wa-heading-m" data-no-anchor data-no-outline>
              Pro Workspace Projects
            </h3>
            <table class="wa-hover-rows">
              <thead>
                <tr>
                  <th scope="col">Step</th>
                  <th scope="col">Action</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td>1</td>
                  <td>Go to your project's <wa-tag class="tag-ui" appearance="outlined"><wa-icon name="gear" variant="regular"></wa-icon> Settings</wa-tag>.
                  </td>
                </tr>
                <tr>
                  <td>2</td>
                  <td>Press <wa-tag class="tag-ui" appearance="outlined"><wa-icon name="paintbrush" variant="regular"></wa-icon> Edit Your Theme</wa-tag> to
                    open
                    the Theme Builder.</td>
                </tr>
                <tr>
                  <td>3</td>
                  <td>Open <wa-tag class="tag-ui" appearance="outlined">Colors</wa-tag> and select <wa-tag class="tag-ui" appearance="outlined">{{ palette.name | capitalize }}</wa-tag>.</td>
                </tr>
                <tr>
                  <td>4</td>
                  <td>Save your theme to immediately update anywhere you're using your project.</td>
                </tr>
              </tbody>
            </table>
          </div>
          {% else %}
          <div class="instruction-group wa-stack wa-gap-2xs">
            <h3 class="wa-heading-m" data-no-anchor data-no-outline>
              Free Workspace Projects
            </h3>
            <table class="wa-hover-rows">
              <thead>
                <tr>
                  <th scope="col">Step</th>
                  <th scope="col">Action</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td>1</td>
                  <td>Go to your project's <wa-tag class="tag-ui" appearance="outlined"><wa-icon name="gear" variant="regular"></wa-icon> Settings</wa-tag>.
                  </td>
                </tr>
                <tr>
                  <td>2</td>
                  <td>Find <wa-tag class="tag-ui" appearance="outlined">Color Palette</wa-tag>. Select <wa-tag
                      class="tag-ui" appearance="outlined"><wa-icon name="swatchbook"
                        variant="regular"></wa-icon>
                      {{ palette.name | capitalize }}</wa-tag>.
                  </td>
                </tr>
                <tr>
                  <td>3</td>
                  <td>Save your theme to immediately update anywhere you're using your project.</td>
                </tr>
              </tbody>
            </table>
          </div>

          <div class="instruction-group wa-stack wa-gap-2xs">
            <h3 class="wa-heading-m" data-no-anchor data-no-outline>
              Pro Workspace Projects
            </h3>
            <table class="wa-hover-rows">
              <thead>
                <tr>
                  <th scope="col">Step</th>
                  <th scope="col">Action</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td>1</td>
                  <td>Go to your project's <wa-tag class="tag-ui" appearance="outlined"><wa-icon name="gear" variant="regular"></wa-icon> Settings</wa-tag>.
                  </td>
                </tr>
                <tr>
                  <td>2</td>
                  <td>Press <wa-tag class="tag-ui" appearance="outlined"><wa-icon name="paintbrush" variant="regular"></wa-icon> Edit Your Theme</wa-tag> to
                    open
                    the Theme Builder.</td>
                </tr>
                <tr>
                  <td>3</td>
                  <td>Open <wa-tag class="tag-ui" appearance="outlined">Colors</wa-tag> and select <wa-tag class="tag-ui" appearance="outlined">{{ palette.name | capitalize }}</wa-tag>.</td>
                </tr>
                <tr>
                  <td>4</td>
                  <td>Save your theme to immediately update anywhere you're using your project.</td>
                </tr>
              </tbody>
            </table>
          </div>
          {% endif %}
        </div>
      </wa-tab-panel>
    </wa-tab-group>
  </div>
  {% endfor %}
</div>

<h2>Styling with Palette Colors</h2>

<p>Once a palette is loaded, you can reach for any of its hue and tint tokens in your own CSS. Pair a hue's <a
    href="/docs/tokens/color#core-colors">core color</a> (<code>--wa-color-{hue}</code>) with its matching on color
  (<code>--wa-color-{hue}-on</code>) to keep text and icons readable on top of it:</p>

{% markdown %}
```css
.callout-custom {
  background: var(--wa-color-blue);
  color: var(--wa-color-blue-on);
}
```
{% endmarkdown %}

{% markdown %}
:::info
**Building with other tints?** The [Color tokens](/docs/tokens/color) reference lists every tint and semantic variant — pair them across the scale and you'll hit WCAG contrast ratios without doing the math.
:::
{% endmarkdown %}
